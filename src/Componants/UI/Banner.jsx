import React from 'react'

const Banner = ({title,subtitle,description}) => {
  return (
    <div>
        <div>
            {title}
            {subtitle}
            {description}
    
      </div>
    </div>
  )
}

export default Banner
